import { RegisterLayout } from "../features/auth/register-layout";

const Register = () => {
  return (
    <div className="h-full w-full bg-primary flex justify-center items-center">
      <RegisterLayout />
    </div>
  );
};

export default Register;
